import {createContext,useContext,useEffect,useState} from 'react';import {authApi} from '../services/api';
const AuthContext=createContext(null);export const useAuth=()=>useContext(AuthContext);
export function AuthProvider({children}){const [user,setUser]=useState(null),[loading,setLoading]=useState(true);useEffect(()=>{if(!localStorage.getItem('quizzy_token'))return setLoading(false);authApi.me().then(x=>setUser(x.user)).catch(()=>localStorage.removeItem('quizzy_token')).finally(()=>setLoading(false))},[]);const accept=x=>{localStorage.setItem('quizzy_token',x.token);setUser(x.user);return x.user};const login=async data=>accept(await authApi.login(data));const register=async data=>accept(await authApi.register(data));const logout=()=>{localStorage.removeItem('quizzy_token');setUser(null)};return <AuthContext.Provider value={{user,loading,login,register,logout}}>{children}</AuthContext.Provider>}
