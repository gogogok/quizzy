import {NavLink,useNavigate} from 'react-router-dom';import {Logo} from '../Header';import {useAuth} from '../../context/AuthContext';
const links=[['⌂','Главная','/dashboard'],['▣','Мои квизы','/quizzes'],['▤','Создать квиз','/quiz/create'],['⌨','Присоединиться','/join'],['♙','Профиль','/profile']];
export default function Sidebar(){const {logout}=useAuth(),nav=useNavigate();return <aside className="sidebar"><Logo/><nav>{links.map(([icon,name,to])=><NavLink key={to} end className={({isActive})=>`side-link ${isActive?'active':''}`} to={to}><span className="side-icon">{icon}</span>{name}</NavLink>)}</nav><button className="side-link sidebar-logout" onClick={()=>{logout();nav('/')}}><span className="side-icon">↪</span>Выйти</button></aside>}
export function Shell({children}){return <div className="shell"><Sidebar/><main className="main">{children}</main></div>}
