import {useState} from 'react';import {useNavigate} from 'react-router-dom';import {Logo} from '../components/Header';import {emitAck} from '../services/socket';
export default function JoinQuizPage(){const nav=useNavigate(),[code,setCode]=useState(''),[nickname,setNickname]=useState(''),[error,setError]=useState('');async function join(){try{const result=await emitAck('room:join',{roomCode:code,nickname});sessionStorage.setItem('roomCode',code.toUpperCase());sessionStorage.setItem('nickname',nickname);sessionStorage.setItem('participantId',result.participant.id);nav('/lobby')}catch(e){setError(e.message)}}return <div className="join"><div className="join-box"><Logo/><h1 style={{marginTop:65}}>Присоединиться к квизу</h1><p style={{color:'#adb3d2'}}>Введите код комнаты, чтобы подключиться к игре</p><div className="field" style={{textAlign:'left'}}><label>Код комнаты</label><input className="input code-field" maxLength="5" value={code} onChange={e=>setCode(e.target.value.toUpperCase())} placeholder="7XK3B"/></div><div className="field" style={{textAlign:'left'}}><label>Ваше имя</label><input className="input dark-input" value={nickname} onChange={e=>setNickname(e.target.value)} placeholder="Иван"/></div>{error&&<p className="form-error">{error}</p>}<button onClick={join} disabled={code.length!==5||nickname.length<2} className="btn primary wide">Присоединиться</button><div className="trophy" style={{fontSize:100,marginTop:35}}>🏆</div></div></div>}
