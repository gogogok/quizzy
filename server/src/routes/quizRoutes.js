import {Router} from 'express';import {auth,organizerOnly} from '../middleware/auth.js';import {listQuizzes,getQuiz,createQuiz,updateQuiz,deleteQuiz} from '../controllers/quizController.js';import {createSession,sessionResults,history} from '../controllers/sessionController.js';const r=Router();r.use(auth,organizerOnly);r.get('/',listQuizzes);r.get('/history',history);r.get('/:id',getQuiz);r.post('/',createQuiz);r.put('/:id',updateQuiz);r.delete('/:id',deleteQuiz);r.post('/:quizId/sessions',createSession);r.get('/sessions/:id/results',sessionResults);export default r;
